// app.js
const INOTForm = () => {
  return (
    <div className="text-center">
      <h1 className="text-2xl font-bold">Planilla INOT</h1>
      <p>Esto es solo un texto de prueba para verificar si React está funcionando correctamente.</p>
    </div>
  );
};

ReactDOM.render(<INOTForm />, document.getElementById('root'));
